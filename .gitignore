.vercel
.env*.local
dist/
node_modules/
.claude/
supabase/.temp/
# Downloaded Strava brand-asset packs (the SVGs we use are copied into images/strava/)
1.1-Connect-with-Strava-Buttons/
1.2-Strava-API-Logos/
1.1-Connect-with-Strava-Buttons.zip
1.2-Strava-API-Logos.zip

# Personal Huawei Health sleep export — served only via the owner-gated
# /api/sleep endpoint, never committed and never served as a static file.
private/

# Raw Huawei Health export (personal health data, ~190MB)
HUAWEI_HEALTH_*.zip

# Vendored agent skills — restore with `npx skills add` (pinned by skills-lock.json)
.agents/
